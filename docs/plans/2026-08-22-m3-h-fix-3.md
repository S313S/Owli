# M3-h-fix-3 实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**目标：** 修复规划期 P1–P6，使实体采集覆盖可判定、规划短流按合法产物判成功，并让同 goal 章节 spec 并发生成。

**架构：** 模型只提供 `subjects`、agent 名称和章结构，实体、能力、ID、依赖与校验仍由系统确定性装配。lint 只读结构化字段；Claude 规划适配层先校验产物，再用错误旗标归因；章节 spec 的输入路径和上游摘要由计划树派生。

**技术栈：** Python 3.12、dataclass、asyncio、Claude Agent SDK、jsonschema、pytest。

---

### 任务 1：实体字段、subjects 与规则 21/25/26/23

**文件：**
- 修改：`app/plan/model.py`
- 修改：`app/plan/generate.py`
- 修改：`app/plan/lint.py`
- 测试：`tests/test_m3h_fix3.py`
- 测试：`tests/test_plan_model.py`

1. 写失败测试：同源不同实体放行、同源同实体拦截且点名实体。
2. 写失败测试：fast 章数预算与合法出路进入 prompt；骨架 `subjects` 与 justification 被校验和保存。
3. 写失败测试：规则 25 在无章计划中点名缺失实体；规则 26 对输入可达实体超集报错。
4. 写失败测试：无章的 `data_collection` agent 使用不适用源时触发规则 23。
5. 运行定向测试，确认失败原因分别是字段/规则缺失。
6. 为 Plan 增加 `subjects` / `subjects_justification`，为 Agent 增加可空 `entity`，旧快照使用兼容缺省。
7. 在 `_build_agent` 用 `partition("·")` 提取采集实体；非采集实体写 null。
8. 同步骨架/goal prompt、scaffold 校验和计划装配；规则 21 改用 `(source_id, entity)`。
9. 实现只读结构字段的规则 25/26，并把规则 23 判断前移到 agent_kind。
10. 运行定向测试直至全绿。

### 任务 2：规划短流按产物判成败

**文件：**
- 修改：`app/adapters/claude.py`
- 修改：`requirements.txt`
- 测试：`tests/test_m3h_fix3.py`

1. 写失败测试：`is_error=True` 但 JSON object 且 schema 合法时成功。
2. 写失败测试：产物不可用时保留 cause；正常 stop_sequence 组合不归 engine_error。
3. 写失败测试：完整 ResultMessage 写入注入的 engine error 日志目录。
4. 运行定向测试确认 RED。
5. 收集文本与 structured_output 后先解析 object，并在有 schema 时用 jsonschema 校验。
6. 只有产物无效时再归因；正常 stop_sequence 组合归为不耗段预算的 service/transport 类原因。
7. 用现有 `append_engine_error` 原样写 ResultMessage，不截断。
8. 运行定向测试直至全绿。

### 任务 3：同 goal 章节 spec 全并发

**文件：**
- 修改：`app/plan/chapters.py`
- 测试：`tests/test_m3h_chapters.py`

1. 把旧的“下一章读取实际 closing”用例改成“线性依赖仍并发且 prompt 使用派生上游信息”。
2. 运行用例，确认当前波次调度只能达到并发度 1。
3. 为每章从 agent 依赖、声明输入、系统实体和 output.path 派生上游摘要。
4. 每个 goal 一次 `asyncio.gather` 生成全部章，不设置 expected_count 串行白名单。
5. 运行章节定向测试直至全绿。

### 任务 4：完整验收

1. 运行 M3-h-fix-3 定向测试与相关旧回归。
2. 运行全量 `../Owli/.venv/bin/python -m pytest -q`，记录尾行。
3. 审计 `source_mcp.py` 未改、fix-2 三处未改、禁用执行期让路/断路关键词、`app/` 无题目硬编码。
4. 对照用户六条验收逐项报告 PASS/FAIL；真实 Claude 单终端夹具由用户实跑，本终端不冒充实跑证据。
