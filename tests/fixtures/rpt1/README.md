# rpt1 分片夹具

`02-关键发现.md` 是 2026-09-06 那轮 `r-3e04f808dffd` 咨询体正式稿真写成的「关键发现」一节（8 432 B、4 个二级标题），从 `exports/r-3e04f808dffd.polished.consulting.md` 里按一级标题切出来的。

**为什么要存进版本库**：原始的 `goals/polished/consulting-parts/02-关键发现.md` 已经被 09-07 那轮 `polish()` 的「每次尝试前清当前这一节」删掉了（那一轮两次都没写出来），树上不再有。分片的往返判据要求「合并回来逐字节相同」，拿真稿量才算数，所以在这里留一份。

`01-执行摘要.md` 是 09-07 15:45 新写成的那一份（4 条发现，各带【B】等级与角标），分片条数就是从它里面读出来的。
